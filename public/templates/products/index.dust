{>"layouts/master" /}

{<title}
Products
{/title}

{<body}
    {>"partials/sidebar" /}
    <div id="main" class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        {?products}
            {#.products}
                <div class="row">
                    <h2>{.title}</h2>
                    <img src="{.image}?dim=1000x200" alt="{.title}">
                </div>
            {/.products}
        {/products}
    </div>
{/body}
